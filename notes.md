## Notes

### Changelog

### Warnings
- To have a word count, in TeXShop → Preferences (or press Cmd + ,), I went to the Engine tab and changed the command `pdflatex --shell-escape --file-line-error --synctex=1` i.e. adding the `--shell-escape` parameter.
